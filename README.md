## Steps to reproduce

1. Install the npm dependencies using `npm i`
2. Start the server using `npm start`
3. Open the Chrome or Safari and go to go to `http://localhost:3000`
4. Load the page
5. Observe that second mock request produces a 404 error
